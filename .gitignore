# Rust
target/

# Models
*.gguf
*.bin

# Logs
logs/
*.log

# Локальная конфигурация: абсолютные пути к моделям, своя на каждой машине.
# В репозитории живёт только kaic.toml.example с плейсхолдерами.
kaic.toml

# Секреты. Репозиторий публичный — любой токен, попавший в коммит, считается
# скомпрометированным навсегда, даже если коммит потом удалить.
# Токены передаются ТОЛЬКО через переменные окружения (TELOXIDE_TOKEN).
ТУТ ТОКЕН.txt
*ТОКЕН*.txt
*token*.txt
*.secret
.env
.env.*

# Рантайм-данные, а не код: кэш скачанных медиаматериалов, журналы отправок
# Telegram, аудит записи файлов. Разрастается, машинно-специфичен и содержит
# материалы чужого авторства — в версионный контроль не идёт.
.kaic/

# IDE
.vscode/
.idea/

# OS
.DS_Store
Thumbs.db